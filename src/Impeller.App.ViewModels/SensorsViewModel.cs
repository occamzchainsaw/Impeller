namespace Impeller.App.ViewModels;

/// <summary>Every sensor the engine can see, across all providers.</summary>
public sealed partial class SensorsViewModel : PageViewModel
{
    /// <inheritdoc />
    public override string Title => "Sensors";
}
